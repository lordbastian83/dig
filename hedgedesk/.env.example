# Copy to .env and fill in. Never commit real keys.
ANTHROPIC_API_KEY=sk-ant-...        # Claude Fable reasoning engine
OPENBB_PAT=...                       # OpenBB personal access token (data)
HEDGEDESK_MODEL=claude-fable-5
# Optional hosted Hermes learning service (otherwise Hermes runs local):
HERMES_ENDPOINT=
HERMES_API_KEY=
